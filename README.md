# Yellow page parser

A simple yellow page parser using `playwright` + `lxml` and my personal helper package `navigator`.

This only parses five pages of restaurants and stores it in a CSV file

Run with:

`uv run ywlp_restaurants`

data will be saved in `output.csv`